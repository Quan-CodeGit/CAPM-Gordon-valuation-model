# Core Flask dependencies
flask>=3.0.0
flask-cors>=4.0.0
gunicorn>=21.2.0

# HTTP and parsing
requests>=2.31.0
beautifulsoup4>=4.12.2
lxml>=4.9.0

# Data processing
pandas>=2.0.0
numpy>=1.24.0

# Stock data APIs
yfinance>=0.2.28

# TradingView scraper (for beta and price data)
tradingview-scraper>=0.5.0

# Vietnamese stock data (optional - will gracefully fail if not available)
# Note: vnstock3 may not install on all platforms
vnstock3>=0.1.5
